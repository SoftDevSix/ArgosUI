import { Typography } from "@mui/material";
import React from "react";

const Home: React.FC = () => {
  return (
    <div>
      <Typography>Home Page</Typography>
    </div>
  );
};

export default Home;
