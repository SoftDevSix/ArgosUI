services:
  argos_ui_prod:
    image: argoscs/ui:latest
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "5175:80"
    volumes:
      - .:/app
      - /app/node_modules
