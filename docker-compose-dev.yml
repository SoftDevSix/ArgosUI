services:
  argos_ui_dev:
    image: argos-ui-dev:latest
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "5173:80"
    volumes:
      - .:/app
      - /app/node_modules